python -m venv venv    # Create a virtual environment named 'venv'
venv\Scripts\activate.bat  # Activate the virtual environment (on Linux/macOS)
pip install -r requirements.txt  # Install project dependencies into the virtual environment
python app.py  # Run your application using the isolated environment
deactivate  # Deactivate the virtual environment when done
